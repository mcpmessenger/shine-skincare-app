# Minimal requirements for fallback deployment
# Use when full ML dependencies fail to install

# Core Flask application
Flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0
python-dotenv==1.0.0
Werkzeug==3.0.1

# Essential utilities
psycopg2-binary==2.9.7
requests==2.31.0
urllib3==2.0.7

# Basic ML support (lightweight)
numpy==1.24.3
Pillow==10.0.1

# Google Cloud (essential for image analysis)
google-cloud-vision==3.4.4
google-auth==2.23.4
google-api-core==2.12.0

# Database
supabase==1.0.4
postgrest==0.10.8

# Monitoring
structlog==23.1.0
psutil==5.9.6

# Note: This configuration will use mock services for:
# - FAISS vector search (falls back to simple similarity)
# - Advanced ML classification (uses basic classification)
# - Heavy image processing (uses Google Vision only)