# Shine Skincare App - Simplified EB Requirements
# Essential dependencies only - for fast, reliable deployment

# Core web framework
Flask==2.3.3
Flask-CORS==4.0.0

# WSGI server for production
gunicorn==21.2.0

# Basic image processing (lighter than OpenCV)
Pillow==10.0.1

# Scientific computing (lighter than full ML stack)
numpy==1.24.3

# Computer vision for face detection
opencv-python==4.8.1.78

# Essential ML libraries
scikit-learn==1.3.0
tensorflow==2.13.0

# Environment variable management
python-dotenv==1.0.0

# AWS SDK (for ML model loading from S3)
boto3==1.34.0

# HTTP requests
requests==2.31.0
