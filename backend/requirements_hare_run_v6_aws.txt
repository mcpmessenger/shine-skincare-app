# 🐇 HARE RUN V6 AWS-COMPATIBLE REQUIREMENTS
# Ultra-fast, aggressive training for maximum accuracy
# Optimized for AWS Elastic Beanstalk deployment

# Core ML Framework (AWS-compatible versions)
tensorflow==2.12.0
keras==2.12.0

# Data Processing (Essential only)
numpy==1.21.6
Pillow==9.5.0
opencv-python-headless==4.8.0.76  # AWS-compatible (no GUI)

# Machine Learning (Essential)
scikit-learn==1.3.0
pandas==1.5.3

# Visualization (AWS-compatible)
matplotlib==3.7.1  # Uses 'Agg' backend for AWS
seaborn==0.12.2

# Utilities (Essential only)
pathlib2==2.3.7

# AWS-Specific Optimizations
# - No GPU dependencies (CPU-first)
# - No heavy augmentation libraries
# - Memory-efficient versions
# - Compatible with Python 3.9-3.11
