flask==3.1.1
flask-cors==3.0.10
gunicorn==21.2.0
python-dotenv==1.0.0
requests==2.31.0

# Rollback Deployment: NO AI dependencies (proven stable)
# numpy==1.24.3          # ❌ Removed - causes startup failures
# pillow==10.1.0          # ❌ Removed - causes startup failures
# opencv-python-headless==4.8.1.78  # ❌ Removed - causes startup failures
# scikit-learn==1.3.2     # ❌ Removed - causes startup failures
# pandas==2.1.4           # ❌ Removed - causes startup failures
# faiss-cpu==1.7.4        # ❌ Removed - causes startup failures
# timm==0.9.12            # ❌ Removed - causes startup failures
# transformers==4.35.0    # ❌ Removed - causes startup failures
# torch==2.1.0            # ❌ Removed - causes startup failures

# Rollback Deployment: Performance and monitoring
psutil==5.9.6
