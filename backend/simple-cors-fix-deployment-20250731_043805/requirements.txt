flask==3.1.1
flask-cors==3.0.10
gunicorn==21.2.0
python-dotenv==1.0.0
requests==2.31.0

# Simple CORS Fix: Core web dependencies only (SAME as ultra minimal)
# NO ML dependencies for maximum stability
