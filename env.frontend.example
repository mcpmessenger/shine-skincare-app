# =============================================================================
# SHINE SKINCARE APP - FRONTEND ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# DO NOT commit your actual .env.local file to version control

# =============================================================================
# NEXT.JS APPLICATION CONFIGURATION
# =============================================================================

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:5000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Application Information
NEXT_PUBLIC_APP_NAME=Shine
NEXT_PUBLIC_APP_DESCRIPTION=AI-Powered Skincare Recommendations
NEXT_PUBLIC_APP_VERSION=1.0.0

# =============================================================================
# GOOGLE OAUTH CONFIGURATION (for frontend)
# =============================================================================

# Google OAuth Client ID (from Google Cloud Console)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-oauth-client-id-here

# Google OAuth Redirect URI (must match backend configuration)
NEXT_PUBLIC_GOOGLE_REDIRECT_URI=http://localhost:3000/auth/callback

# =============================================================================
# STRIPE CONFIGURATION (for frontend)
# =============================================================================

# Stripe Publishable Key (for frontend operations)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# Stripe Price IDs (for subscription tiers)
NEXT_PUBLIC_STRIPE_BASIC_PRICE_ID=price_your_basic_tier_price_id
NEXT_PUBLIC_STRIPE_PREMIUM_PRICE_ID=price_your_premium_tier_price_id
NEXT_PUBLIC_STRIPE_PRO_PRICE_ID=price_your_pro_tier_price_id

# =============================================================================
# SUPABASE CONFIGURATION (for frontend)
# =============================================================================

# Supabase Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous Key (for frontend operations)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# =============================================================================
# ANALYTICS CONFIGURATION
# =============================================================================

# Google Analytics (optional)
NEXT_PUBLIC_GA_TRACKING_ID=G-XXXXXXXXXX

# Mixpanel (optional)
NEXT_PUBLIC_MIXPANEL_TOKEN=your_mixpanel_token

# =============================================================================
# FEATURE FLAGS (for frontend)
# =============================================================================

# Enable/Disable Frontend Features
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_DEBUG_MODE=true
NEXT_PUBLIC_ENABLE_GOOGLE_VISION=true
NEXT_PUBLIC_ENABLE_FAISS_SEARCH=true
NEXT_PUBLIC_ENABLE_STRIPE_PAYMENTS=true

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================

# Development Mode
NODE_ENV=development

# Debug Configuration
NEXT_PUBLIC_DEBUG=true
NEXT_PUBLIC_LOG_LEVEL=debug

# =============================================================================
# PRODUCTION CONFIGURATION (override these in production)
# =============================================================================

# Production Settings (set these in production)
# NEXT_PUBLIC_API_URL=https://api.your-domain.com
# NEXT_PUBLIC_APP_URL=https://your-domain.com
# NEXT_PUBLIC_GOOGLE_REDIRECT_URI=https://your-domain.com/auth/callback
# NODE_ENV=production
# NEXT_PUBLIC_DEBUG=false

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# 1. Copy this file to .env.local:
#    cp env.frontend.example .env.local

# 2. Update the values in .env.local with your actual credentials

# 3. Required services to set up:
#    - Google Cloud Console (OAuth)
#    - Stripe (for payments)
#    - Supabase (for database and storage)
#    - Analytics services (optional)

# 4. Test your configuration:
#    npm run dev
#    # Visit: http://localhost:3000

# =============================================================================
# SECURITY NOTES
# =============================================================================

# ⚠️  IMPORTANT SECURITY REMINDERS:
# - Never commit your .env.local file to version control
# - Only use NEXT_PUBLIC_ prefix for variables that need to be exposed to the browser
# - Keep sensitive keys (like Stripe secret keys) in the backend only
# - Use environment-specific configurations
# - Monitor API usage and costs 